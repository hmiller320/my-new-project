const emoji = require("node-emoji");

let coffee = emoji.get('coffee');
console.log(coffee);

/* 
function emojiLoop = (array){
    for (let i = 0; i <= array.length; i++){
        return console.log(emoji[i])
    }

} 
*/
